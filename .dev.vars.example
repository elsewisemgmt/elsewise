# Copy to .dev.vars for `npm run preview:worker` (wrangler dev), which reads
# .dev.vars rather than .env. Same four values as .env.example.
KEYSTATIC_GITHUB_CLIENT_ID=
KEYSTATIC_GITHUB_CLIENT_SECRET=
KEYSTATIC_SECRET=
PUBLIC_KEYSTATIC_GITHUB_APP_SLUG=
